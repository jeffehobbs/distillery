# Copy to secrets.txt (chmod 600) and fill in. Never commit the real file.
# Only needed for --library, which pulls albums off an SMB share.
SMB_HOST=nas.local
SMB_SHARE=music
SMB_USER=your-username
SMB_PASSWORD=your-password
